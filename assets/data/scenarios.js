/*
 * Static "level" scenarios for Practice Scenarios (27 total).
 * Each entry is either null (locked / not authored yet) or:
 *   { title, description, strips: [ { type, spaces: {"3":"...", ...} }, ... ] }
 *
 * These are baked into the site (same for everyone). Author them with the
 * temporary tool on the Practice Scenarios page, then paste the exported JSON
 * into the `scenarios` array below and commit.
 */
window.ZAE_STATIC_SCENARIOS = {
  total: 27,
  scenarios: [
    // 1-27  (fill in as authored; null = locked)
    null, null, null, null, null, null, null, null, null,
    null, null, null, null, null, null, null, null, null,
    null, null, null, null, null, null, null, null, null
  ]
};
